image: alpine/3.11
packages:
    - cargo
    - openssl
    - openssl-dev
sources:
    - https://git.sr.ht/~taiite/ellidri
tasks:
    - build: |
        cd ellidri
        cargo build
    - test: |
        cd ellidri
        cargo test
